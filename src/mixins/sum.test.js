import sum from './sum';

test('adds 1 + 2 is equalt to 3', () => {
  expect(sum(1, 2)).toBe(3);
});

test('adds 1 + 2 is equalt to 3', () => {
  expect(sum(1, 2)).toBe(4);
});

test('adds 1 + 2 is equalt to 3', () => {
  expect(sum(1, 2)).toBe(5);
});
